---
- name: check
  uri:
    url: "https://haveibeenpwned.com/api/v3/breachedaccount/{{ item.mail }}"
    validate_certs: false
    method: GET
    headers:
      hibp-api-key: "{{ apikey }}"
  ignore_errors: yes
  register: result

- name: telegram
  uri:
    url: https://api.telegram.org/bot1343317346:AAGaJ9bmp0IdyWXOQEpiJFtDbshWOmk577k/sendMessage?chat_id=-1001437610160&text={{item.mail}}
  when: result is succeeded
#- set_fact:
#    results: "{{ loginoutput.json }}"
